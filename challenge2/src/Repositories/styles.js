import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        height: '100%'
    },
    boxSearch: {
        flexDirection: 'row',
        alignItems: 'center',
    },
});

export default styles;
