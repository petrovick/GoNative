import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
      flex: 1,
      backgroundColor: `#333`
  },
  boxSearch: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'stretch',
    backgroundColor: `#999`
  },
});

export default styles;
